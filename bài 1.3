#include<stdio.h>

struct toado{
	int tungdo;
	int hoanhdo;
};

void nhaptoado(toado &l)
{
	 printf(" Nhap hoanh do va tung do: ");
	 scanf("%d%d", &l.hoanhdo, &l.tungdo)
}

void xuattoado(toado l)
{
	printf("hoanh do va toa do: ", hoanhdo, tungdo);
}

int main(){
	toaDo l;
	nhaptoaDo(l);
	xuattoaDo(l);
	return 0;
}
